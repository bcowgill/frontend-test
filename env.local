# source this file to begin work on the project
export PATH="./node_modules/.bin:$PATH"
nvm use
# build tools to run in terminals:
# keep-it-up.sh yarn start
# yarn server
# auto-build.sh "npm run prettier" src
# yarn test
